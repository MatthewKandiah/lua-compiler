#include "gtest/gtest.h"
#include "../src/parser.h"

// TODO - add a mock lexer so we can pass an arbitrary series of tokens into the parser for testing

TEST(ParserTests, TestThatShouldPass) {
  EXPECT_EQ(1, 1);
}
